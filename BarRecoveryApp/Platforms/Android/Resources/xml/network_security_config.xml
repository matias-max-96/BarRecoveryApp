<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
	<!--
        Solo para desarrollo/piloto interno: permite tráfico HTTP sin cifrar
        hacia el emulador (10.0.2.2 = tu PC visto desde el emulador Android)
        y hacia localhost. NO agregar dominios de producción reales aquí —
        antes de desplegar el backend central "de verdad" (fuera del
        emulador/tu red de pruebas), este archivo debe volver a exigir HTTPS
        con un certificado válido, no autofirmado.
    -->
	<domain-config cleartextTrafficPermitted="true">
		<domain includeSubdomains="false">10.0.2.2</domain>
		<domain includeSubdomains="false">localhost</domain>
	</domain-config>
</network-security-config>