<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BarRecoveryApp.Views.BarImportPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Importar barras">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">

            <Label
                Text="Importación Masiva de Barras"
                FontSize="26"
                FontAttributes="Bold"
                HorizontalOptions="Center" />

            <Label
                Text="Seleccione un archivo CSV con las columnas BarNumber, PlantCode y BarTypeCode."
                FontSize="14"
                TextColor="Gray"
                HorizontalTextAlignment="Center" />

            <!-- ActivityIndicator quitado: bug de renderizado en Android,
                 ver LoginPage.xaml para el detalle. -->

            <!-- Error general -->
            <Border
                IsVisible="{Binding HasError}"
                Style="{StaticResource ErrorBanner}">

                <Label
                    Text="{Binding ErrorMessage}"
                    Style="{StaticResource ErrorBannerText}" />
            </Border>

            <!-- Éxito -->
            <Border
                IsVisible="{Binding HasSuccess}"
                Style="{StaticResource SuccessBanner}">

                <Label
                    Text="{Binding SuccessMessage}"
                    Style="{StaticResource SuccessBannerText}" />
            </Border>

            <Label
                Text="Formato esperado"
                FontSize="20"
                FontAttributes="Bold" />

            <Border
                Padding="12"
                Stroke="#DDDDDD"
                StrokeThickness="1"
                BackgroundColor="#F8F9FA">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="4">

                    <Label
                        Text="BarNumber,PlantCode,BarTypeCode"
                        FontFamily="monospace"
                        FontSize="13" />

                    <Label
                        Text="BARRA-001,ANA,60"
                        FontFamily="monospace"
                        FontSize="13" />

                    <Label
                        Text="BARRA-002,VALDIVIA,90"
                        FontFamily="monospace"
                        FontSize="13" />

                    <Label
                        Text="MAPA-001,MAPA,MAPA"
                        FontFamily="monospace"
                        FontSize="13" />

                </VerticalStackLayout>
            </Border>

            <Label
                Text="Archivo seleccionado"
                FontSize="20"
                FontAttributes="Bold" />

            <Label
                Text="{Binding SelectedFileName}"
                FontSize="14"
                TextColor="Gray" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">

                <Button
                    Grid.Column="0"
                    Text="Limpiar"
                    Command="{Binding ClearCommand}"
                    Style="{StaticResource OutlineButton}" />

                <Button
                    Grid.Column="1"
                    Text="Seleccionar e importar"
                    Command="{Binding PickAndImportCommand}" />

            </Grid>

            <BoxView
                HeightRequest="1"
                Color="#DDDDDD"
                Margin="0,8" />

            <Label
                Text="Resumen"
                FontSize="20"
                FontAttributes="Bold" />

            <Label
                Text="{Binding SummaryText}"
                FontSize="14"
                TextColor="Gray" />

            <Label
                Text="Errores / advertencias"
                FontSize="20"
                FontAttributes="Bold" />

            <CollectionView ItemsSource="{Binding Errors}">
                <CollectionView.EmptyView>
                    <Label
                        Text="No hay errores para mostrar."
                        TextColor="Gray"
                        HorizontalTextAlignment="Center"
                        Margin="0,12" />
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ErrorBanner}">

                            <VerticalStackLayout Spacing="4">

                                <Label
                                    Text="{Binding RowNumber, StringFormat='Fila: {0}'}"
                                    FontSize="14"
                                    Style="{StaticResource ErrorBannerText}" />

                                <Label
                                    Text="{Binding BarNumber, StringFormat='Barra: {0}'}"
                                    FontSize="13"
                                    TextColor="Gray" />

                                <Label
                                    Text="{Binding Message}"
                                    FontSize="13"
                                    Style="{StaticResource ErrorBannerText}" />

                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>