<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BarRecoveryApp.Views.RecoveryRecordPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Registrar recuperación">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">

            <Label
                Text="Registrar Recuperación"
                FontSize="26"
                FontAttributes="Bold"
                HorizontalOptions="Center" />

            <ActivityIndicator
                IsRunning="{Binding IsBusy}"
                IsVisible="{Binding IsBusy}"
                HorizontalOptions="Center" />

            <Border
                IsVisible="{Binding HasError}"
                Padding="12"
                Stroke="#D9534F"
                StrokeThickness="1"
                BackgroundColor="White">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8" />
                </Border.StrokeShape>
                <Label
                    Text="{Binding ErrorMessage}"
                    TextColor="#B00020"
                    FontAttributes="Bold" />
            </Border>

            <Border
                IsVisible="{Binding HasSuccess}"
                Padding="12"
                Stroke="#2E7D32"
                StrokeThickness="1"
                BackgroundColor="White">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8" />
                </Border.StrokeShape>
                <Label
                    Text="{Binding SuccessMessage}"
                    TextColor="#2E7D32"
                    FontAttributes="Bold" />
            </Border>

            <Label
                Text="Datos de actividad"
                FontSize="20"
                FontAttributes="Bold" />

            <Picker
                Title="Seleccione barra"
                ItemsSource="{Binding Bars}"
                SelectedItem="{Binding SelectedBar}"
                ItemDisplayBinding="{Binding BarNumber}" />

            <Picker
                Title="Seleccione actividad"
                ItemsSource="{Binding Activities}"
                SelectedItem="{Binding SelectedActivity}"
                ItemDisplayBinding="{Binding Name}" />

            <Label
                Text="Insumos utilizados"
                FontSize="20"
                FontAttributes="Bold" />

            <Picker
                Title="Seleccione insumo"
                ItemsSource="{Binding Supplies}"
                SelectedItem="{Binding SelectedSupply}"
                ItemDisplayBinding="{Binding Name}" />

            <Entry
                Text="{Binding Quantity}"
                Placeholder="Cantidad utilizada"
                Keyboard="Numeric" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button
                    Grid.Column="0"
                    Text="Agregar insumo"
                    Command="{Binding AddSupplyCommand}"
                    BackgroundColor="#0D6EFD"
                    TextColor="White" />

                <Button
                    Grid.Column="1"
                    Text="Quitar seleccionado"
                    Command="{Binding RemoveSupplyCommand}"
                    BackgroundColor="#DC3545"
                    TextColor="White" />
            </Grid>

            <CollectionView
                ItemsSource="{Binding UsedSupplies}"
                SelectedItem="{Binding SelectedSupplyEntry, Mode=TwoWay}"
                SelectionMode="Single">

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Padding="12"
                            Margin="0,4"
                            Stroke="#DDDDDD"
                            StrokeThickness="1"
                            BackgroundColor="White">

                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>

                            <Label
                                Text="{Binding DisplayText}"
                                FontAttributes="Bold" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label
                Text="Observación"
                FontSize="20"
                FontAttributes="Bold" />

            <Editor
                Text="{Binding Notes}"
                Placeholder="Observación del trabajo realizado"
                AutoSize="TextChanges"
                HeightRequest="100" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button
                    Grid.Column="0"
                    Text="Limpiar"
                    Command="{Binding ClearCommand}"
                    BackgroundColor="#6C757D"
                    TextColor="White" />

                <Button
                    Grid.Column="1"
                    Text="Guardar"
                    Command="{Binding SaveCommand}"
                    BackgroundColor="#198754"
                    TextColor="White" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>