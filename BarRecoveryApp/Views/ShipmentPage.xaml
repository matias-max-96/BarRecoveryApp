<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BarRecoveryApp.Views.ShipmentPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Envíos">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">

            <Label
                Text="Registro de Envío"
                FontSize="26"
                FontAttributes="Bold"
                HorizontalOptions="Center" />

            <!-- ActivityIndicator quitado: bug de renderizado en Android,
                 ver LoginPage.xaml para el detalle. -->

            <Border
                IsVisible="{Binding HasError}"
                Style="{StaticResource ErrorBanner}">

                <Label
                    Text="{Binding ErrorMessage}"
                    Style="{StaticResource ErrorBannerText}" />
            </Border>

            <Border
                IsVisible="{Binding HasSuccess}"
                Style="{StaticResource SuccessBanner}">

                <Label
                    Text="{Binding SuccessMessage}"
                    Style="{StaticResource SuccessBannerText}" />
            </Border>

            <Label
                Text="Datos del envío"
                FontSize="20"
                FontAttributes="Bold" />

            <Entry
                Text="{Binding TransferOrder}"
                Placeholder="Orden de traslado" />

            <Entry
                Text="{Binding CustomerReference}"
                Placeholder="Referencia cliente / destino opcional" />

            <Entry
                Text="{Binding DispatchGuideNumber}"
                Placeholder="N° de guía de despacho (documento agencia externa)" />

            <BoxView HeightRequest="1" Color="#DDDDDD" Margin="0,8" />

            <Label
                Text="Filtros de búsqueda"
                FontSize="20"
                FontAttributes="Bold" />

            <Picker
                Title="Seleccione planta"
                ItemsSource="{Binding Plants}"
                SelectedItem="{Binding SelectedPlant}"
                ItemDisplayBinding="{Binding Name}" />

            <Picker
                Title="Seleccione tipo de barra"
                ItemsSource="{Binding BarTypes}"
                SelectedItem="{Binding SelectedBarType}"
                ItemDisplayBinding="{Binding Name}" />

            <Entry
                Text="{Binding SearchText}"
                Placeholder="Buscar por número, planta o tipo de barra" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">

                <Button
                    Grid.Column="0"
                    Text="Limpiar filtros"
                    Command="{Binding ClearFiltersCommand}"
                    Style="{StaticResource OutlineButton}" />

                <Button
                    Grid.Column="1"
                    Text="Buscar barras"
                    Command="{Binding SearchCommand}" />

            </Grid>

            <Label
                Text="{Binding ResultCountText}"
                FontSize="13"
                TextColor="Gray" />

            <BoxView HeightRequest="1" Color="#DDDDDD" Margin="0,8" />

            <Label
                Text="Resultados de búsqueda"
                FontSize="20"
                FontAttributes="Bold" />

            <CollectionView ItemsSource="{Binding Bars}">
                <CollectionView.EmptyView>
                    <Label
                        Text="No hay barras listas para envío. Use los filtros y presione Buscar barras."
                        TextColor="Gray"
                        HorizontalTextAlignment="Center"
                        Margin="0,24" />
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Padding="12"
                            Margin="0,4"
                            Stroke="#DDDDDD"
                            StrokeThickness="1"
                            BackgroundColor="White">

                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>

                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">

                                <CheckBox
                                    Grid.Column="0"
                                    IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                    CheckedChanged="OnBarCheckedChanged" />

                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label
                                        Text="{Binding DisplayName}"
                                        FontAttributes="Bold"
                                        FontSize="16" />

                                    <Label
                                        Text="{Binding DetailText}"
                                        FontSize="13"
                                        TextColor="Gray" />
                                </VerticalStackLayout>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <BoxView HeightRequest="1" Color="#DDDDDD" Margin="0,8" />

            <Label
                Text="Barras seleccionadas para envío"
                FontSize="20"
                FontAttributes="Bold" />

            <Label
                Text="{Binding SelectedCountText}"
                FontSize="13"
                TextColor="Gray" />

            <CollectionView ItemsSource="{Binding SelectedBars}">
                <CollectionView.EmptyView>
                    <Label
                        Text="No hay barras seleccionadas."
                        TextColor="Gray"
                        HorizontalTextAlignment="Center"
                        Margin="0,12" />
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource SuccessBanner}">

                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">

                                <VerticalStackLayout Grid.Column="0" Spacing="4">
                                    <Label
                                        Text="{Binding DisplayName}"
                                        FontAttributes="Bold"
                                        FontSize="16" />

                                    <Label
                                        Text="{Binding DetailText}"
                                        FontSize="13"
                                        TextColor="Gray" />
                                </VerticalStackLayout>

                                <Button
                                    Grid.Column="1"
                                    Text="Quitar"
                                    Clicked="OnRemoveSelectedBarClicked"
                                    Style="{StaticResource DangerButton}" />

                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Grid
                ColumnDefinitions="*,*,*"
                RowDefinitions="Auto,Auto"
                ColumnSpacing="12"
                RowSpacing="12">

                <Button
                    Grid.Row="0"
                    Grid.Column="0"
                    Text="Limpiar selección"
                    Command="{Binding ClearSelectionCommand}"
                    Style="{StaticResource OutlineButton}" />

                <Button
                    Grid.Row="0"
                    Grid.Column="1"
                    Text="Limpiar todo"
                    Command="{Binding ClearAllCommand}"
                    Style="{StaticResource OutlineButton}" />

                <Button
                    Grid.Row="0"
                    Grid.Column="2"
                    Text="Crear envío"
                    Command="{Binding SaveCommand}" />

                <Border
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    IsVisible="{Binding HasTechnicalReport}"
                    Style="{StaticResource SuccessBanner}">

                    <VerticalStackLayout Spacing="8">

                        <Label
                            Text="Reporte técnico generado"
                            FontSize="18"
                            Style="{StaticResource SuccessBannerText}" />

                        <Label
                            Text="{Binding LastTechnicalReportFileName}"
                            FontSize="14"
                            TextColor="Gray" />

                        <Label
                            Text="{Binding LastTechnicalReportFilePath}"
                            FontSize="12"
                            TextColor="Gray"
                            LineBreakMode="CharacterWrap" />

                        <Button
                            Text="Compartir reporte técnico"
                            Command="{Binding ShareTechnicalReportCommand}"
                            Style="{StaticResource SecondaryButton}" />

                    </VerticalStackLayout>
                </Border>

            </Grid>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>